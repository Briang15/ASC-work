const number = 5;
const string = "brian";
const Boolean = false;

console.log(number + string + Boolean); 

 const amount=(45 * 4 + 75 *2);

 
//copying code for editing (was on the board)
console.log('Cost: ' + (amount));
console.log('selling price: ' + (amount) * 1.5);
console.log('Seller fee: ' + (amount) * 1.5 * 0.2);
console.log('Total profit: ' + ((amount) * 1.5 * 0.2 - (amount)));

